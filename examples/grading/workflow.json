{
  "tools": ["grep", "readFile", "searchFiles"],
  "steps": [
    "read_dependencies",
    ["verify_test_helpers", "verify_mocks_and_stubs"],
    "generate_grades",
    "calculate_final_grade",
    "format_result",
    "generate_recommendations"
  ]
}
